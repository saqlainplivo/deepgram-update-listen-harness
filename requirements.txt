# Core — WebSocket harness
websockets>=12.0
python-dotenv>=1.0.0

# Bridge server (Plivo ↔ Deepgram)
fastapi>=0.110.0
uvicorn[standard]>=0.29.0
python-multipart>=0.0.9
numpy>=1.24.0

# Optional: live microphone input
# sounddevice>=0.4.6
